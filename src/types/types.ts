export interface childType {
  children: React.ReactNode;
}

export interface userContextSchema {
  id: number;
  email: string;
  username: string;
  firstName: string;
  lastName: string;
  token: string;
  image: string;
}
